<Window x:Class="ScreenBoard.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScreenBoard.Views" xmlns:viewmodels="clr-namespace:ScreenBoard.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        WindowStyle="SingleBorderWindow"
        Title="ScreenBoard Config" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Frame Grid.Column="0" Grid.Row="1" Source="Pages/ScreenBoardPage.xaml" />

        <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Stretch">
            <Frame NavigationUIVisibility="Hidden" Grid.Row="1" Source="Pages/ConfigPage.xaml" />
        </StackPanel>
        
    </Grid>
</Window>
